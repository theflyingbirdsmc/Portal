# Name: TFB-Backstage-App
appId: ${GITHUB_APP_ID}
webhookUrl: ${GITHUB_APP_WEBHOOK}
clientId: ${GITHUB_APP_CLIENT_ID}
clientSecret: ${GITHUB_APP_CLIENT_SECRET}
webhookSecret: ${GITHUB_APP_WEBHOOK_SECRET}
privateKey: | 
  ${GITHUB_APP_PRIVATE_KEY}